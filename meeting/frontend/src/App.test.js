/**
 * CRA/Jest cannot resolve react-router-dom@7 "exports" in this environment.
 * Run the app with `npm start` to verify UI; `npm run build` checks compilation.
 */
test("sanity", () => {
  expect(true).toBe(true);
});
